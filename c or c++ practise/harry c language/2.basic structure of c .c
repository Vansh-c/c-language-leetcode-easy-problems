#include <stdio.h>

int main()
{
    printf("lets understand the syntax of c language");
    return 0;
}

// reserve keywords : these are special keywords which cannot be used during inside of our programs ex: int , return ,char ...etc
// identifies: its are name which are given to specified variables etc.like const ,variables, underscore ..etc .real life examples are mom calling you by your nickname

// string literals : whatever written inside double quotation marks inside printf 
//variables: a container to store the given string literals 
//symbols: scanf("%d",&a) where % and & are symbols 